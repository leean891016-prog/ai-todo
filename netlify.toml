[build]
  command = "mkdir -p dist && echo 'ai-todo push backend' > dist/index.html"
  publish = "dist"

[functions]
  node_bundler = "esbuild"

  [functions.cron]
    schedule = "* * * * *"

  [functions.sync]

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/sync"
  status = 200
